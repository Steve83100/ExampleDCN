# Generate configuration files for k=4 FatTree

from mininet.util import irange
import os

def genCisco(k, conf_path = "./confCisco"):
    for i in irange(1, k):
        pass

def genFRR(k, conf_path = "./conf"):
    for i in irange(1, k):
        pass